1. Make sure you reset the database before running. using reset_dev.sh

2. Make sure the dev server is running

3. execute the tests

to run all the tests:

python3 -m pytest

I like running it like this. v for verbose

python3 -m pytest -v
